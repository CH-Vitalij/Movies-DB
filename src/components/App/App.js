import { Flex } from 'antd';

import Frames from '../Frames';
import NetworkState from '../NetworkState';

const App = () => {
  return (
    <Flex justify="center">
      <Frames />
    </Flex>
  );
};

export default App;
